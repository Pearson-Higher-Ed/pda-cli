const { exec } = require('child_process');
const colors   = require('colors');

module.exports = function _generateDocs(){
  console.log(colors.magenta('Generating Component Documentation Page...'));
};
